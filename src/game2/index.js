import system from './System'
import GameObject from './GameObject'
import Room from './Room'

system.classRegister.set('room', Room)
system.classRegister.set('gameobject', GameObject)


export default {
  run(userId) {
    
  }
}